{
  "name": "@sympoies/dsh-applications-workspace",
  "version": "0.3.0",
  "description": "Governed public applications and plugins for DeepSeek Harness",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "files": [
    "AGENTS.md",
    "SECURITY.md",
    "CONTRIBUTING.md",
    "compatibility",
    "docs",
    "fixtures",
    "profiles",
    "packages"
  ],
  "scripts": {
    "test": "node --test test/*.test.ts",
    "test:repository-contract": "node --test test/repository-contract.test.ts",
    "test:manager-contract": "node --test test/manager-contract.test.ts",
    "test:manager-faults": "node --test test/manager-faults.test.ts",
    "test:plugin-sandbox": "node --test test/plugin-sandbox.test.ts",
    "test:profiles": "node --test test/profiles.test.ts",
    "test:profile-compatibility": "node scripts/check-profile-compatibility.ts",
    "test:integration": "node --test test/integration.test.ts",
    "test:exact-dsh": "node scripts/check-exact-dsh-consumer.ts && node --test test/exact-dsh-consumer.test.ts",
    "test:github-contracts": "node --test test/github-contracts.test.ts",
    "check:compatibility": "node scripts/check-compatibility.ts",
    "verify:package-reproducibility": "node scripts/check-package-reproducibility.ts",
    "test:package": "npm run check:compatibility -- --manifest-only && npm run verify:package-reproducibility && npm pack --dry-run --ignore-scripts",
    "test:conversation": "node --test test/conversation.test.ts",
    "test:batch": "node --test test/batch.test.ts",
    "typecheck": "tsc -p tsconfig.json && tsc -p tsconfig.tools.json"
  },
  "engines": {
    "node": ">=24.0.0"
  },
  "packageManager": "npm@11.6.2",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/sympoies/dsh-applications.git"
  },
  "devDependencies": {
    "@types/node": "24.13.3",
    "typescript": "6.0.3"
  }
}
