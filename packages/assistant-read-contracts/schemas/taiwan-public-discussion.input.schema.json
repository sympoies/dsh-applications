{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://sympoies.dev/schemas/dsh-assistant-read/taiwan-public-discussion.input.schema.json",
  "type": "object",
  "additionalProperties": false,
  "required": ["topic", "since", "until", "locale", "sourceClasses", "maxFindings"],
  "properties": {
    "topic": { "type": "string", "minLength": 1, "maxLength": 2048 },
    "since": { "type": "string", "format": "date-time" },
    "until": { "type": "string", "format": "date-time" },
    "locale": { "const": "zh-TW" },
    "sourceClasses": { "type": "array", "minItems": 1, "maxItems": 3, "uniqueItems": true, "items": { "enum": ["forum", "news", "social"] } },
    "maxFindings": { "type": "integer", "minimum": 1, "maximum": 32 }
  }
}
