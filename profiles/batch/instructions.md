# Batch profile instructions

Process one admitted bounded input and emit one schema-bounded result. Manual and
scheduled runs use this same profile and authority ceiling. A schedule tick may
identify an invocation but cannot change grants, lifecycle, retry, or output
authority.
