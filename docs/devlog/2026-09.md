# Development log - 2026-09

## 2026-09-08 - Read results retain request semantics and schema language

**Result**

- Completed weather results must use the exact `metric` or `imperial` unit
  system authorized by the request; an otherwise valid cross-unit result is
  rejected before it can be consumed.
- Weather results retain the authorized request location, and direct timestamp
  validation accepts the same lowercase separators and fractional precision as
  the published RFC 3339 schemas.

**Why / context**

- Exact-head API review found that the validator checked the result enum but
  did not bind it to the request, allowing numerically plausible values with
  the wrong semantics.
- Follow-up API and testing review found the same missing binding for location
  and two schema-valid RFC 3339 spellings rejected by the direct validator.

**Evidence**

- Focused owner tests first failed with mismatched weather units, a substituted
  location, lowercase `t`/`z`, and nanosecond-style fractional timestamps,
  then passed after the binding and parser repairs. All 19 focused cases, the
  repository suite, typecheck, compatibility, reproducible packaging, and
  package dry-run pass.

**Links**

- PR #31
- Issue #26
- `packages/assistant-read-contracts/src/index.ts`

## 2026-09-08 - Web schema rejects encoded private authorities

**Result**

- The digest-bound Web extraction schema now rejects percent-encoded URL
  authority components while continuing to allow ordinary encoding in public
  paths and query strings.
- Schema and direct-validator regressions cover encoded loopback and localhost
  spellings and encoded public hostnames so both admission paths preserve the
  same public-target boundary.

**Why / context**

- Exact-head security review found that the direct validator normalized and
  rejected encoded private targets but the JSON Schema accepted them. A schema
  consumer could therefore admit input the public validator rejects.

**Evidence**

- Before the schema repair, the focused conformance case failed on
  `http://127%2e0%2e0%2e1/`. A second review then reproduced the inverse drift
  with `https://exa%6dple.com/`; after both repairs all 19 focused cases pass
  against the exact locked runtime-kit.

**Links**

- PR #31
- Issue #26
- `packages/assistant-read-contracts/schemas/web.input.schema.json`

## 2026-09-08 - Assistant reads bind results to reviewed authority

**Result**

- Result validation now consumes an immutable authorized invocation carrying
  admission, implementation, binding, and audience identity, and applies the
  request's output, source, and capability-specific result limits.
- Web extraction rejects local and literal targets before execution and
  publishes explicit broker requirements for public DNS answers, redirect-hop
  revalidation, and raw-byte limits before decode.
- Digest-bound schemas and direct validators agree on expressible constraints;
  calendar and freshness validation now reject impossible or reversed dates.

**Why / context**

- Review of the first public contract head found that catalog ceilings and URL
  syntax alone did not preserve the narrower authorization or public-network
  boundary through result validation and provider execution.
- The repair keeps provider clients private while making the portable contract
  sufficient for a broker to match one admitted target and produce bounded,
  chronological evidence.

**Evidence**

- A behavior-level owner-test red reproduced five failures on the published
  head: local targets, impossible dates, lost admission identity, reversed
  freshness, and eager wide-object enumeration.
- The focused suite passes all 19 cases against the exact locked runtime-kit,
  including exchange-rate and extraction variants, schema conformance,
  request-specific limits, and broker-policy negatives.
- Clean install, the full repository suite, typecheck, manifest compatibility,
  reproducible packaging, and package dry-run pass.

**Links**

- PR #31
- Issue #26
- `packages/assistant-read-contracts/README.md`

## 2026-09-08 - Six assistant reads become separately admitted public contracts

**Result**

- A new coordinated `assistant-read-contracts` package defines independent
  weather, market/exchange, Steam catalog, Web lookup/extraction,
  recent-community research, and Taiwan public-discussion actions.
- Every action has a digest-bound strict schema, one capability-specific
  public-data network class, a disabled default, a byte/source/timeout ceiling,
  source freshness, and required cancellation. An exact implementation and
  audience admission cannot activate or widen any sibling action.
- Provider clients and bindings remain outside the public package. Its
  descriptors request only read-class mediation and no filesystem, subprocess,
  credential-handle, project, shell, or ambient-network authority.

**Why / context**

- Bots need reusable contracts for current-data utilities and longer research
  without importing a concrete provider, deployment topology, or a second
  runtime. One aggregate descriptor would couple unrelated network and
  authority grants, so the package instead constructs one descriptor per
  capability.
- Exact-byte ecosystem evaluation found no adoptable contract for five
  families. The ordinary Web contract references the locked official Web
  seam's cancellation and source patterns, while preserving separate
  runtime-kit admission and provider neutrality.

**Evidence**

- Owner tests first failed while the package was absent, then pass all positive
  paths plus wrong capability, implementation, audience, budgets, cancellation,
  leakage, and authority-escalation cases.
- All six descriptors validate against runtime-kit revision
  `2cd14d5fdd73e0758d366d8b671f71ee768d857f`; the exact focused run passes
  11 tests.
- The full repository suite, strict downstream TypeScript consumer, typecheck,
  manifest-only compatibility, reproducible packaging, and package dry-run
  pass. Candidate probes used fresh throwaway DSH homes with lifecycle scripts
  blocked and executed no provider request.

**Links**

- Issue #26
- `packages/assistant-read-contracts/`
- `docs/plugin-evaluations/`

## 2026-09-08 - Harden governed actions at every admission and receipt boundary

**Result**

- Every governed action now owns distinct input and output schema digests, and
  validators bind the payload action to the action separately admitted by the
  host. Receipt families reject sibling-family action IDs.
- Existing-session operations fail closed without the trusted session,
  prior-receipt, and current-state evidence needed for that action. Calendar
  validation now rejects impossible RFC 3339 calendar and time components.
- The calendar descriptor's 256 KiB output ceiling accommodates the complete
  16-event UTF-8 schema maximum. Checked-in schema ownership is verified on
  every test run, while hosted CI runs the focused suite against the exact
  runtime-kit checkout.

**Why / context**

- A family-wide schema could let an admitted read reach a sibling write shape,
  and a family-wide receipt validator could accept an unrelated action label.
  Session results also need to remain attached to the exact live session rather
  than only its workspace and receipt chain.
- The public request reference is the private adapter's exact runtime-kit
  idempotency key for mutations. Runtime-kit remains the sole request-digest
  conflict and replay-journal owner; no second retry or replay mechanism was
  added here.

**Evidence**

- Seven focused regressions first reproduced action confusion, cross-family
  receipts, missing session/state evidence, impossible timestamps, UTF-8 output
  overflow, and an exact-runtime CI coverage gap; the repaired owner suite
  passes all cases.
- TypeScript validation, the full repository suite, exact compatibility,
  reproducible packaging, and dry-run package inspection pass.

**Links**

- PR #30
- `packages/governed-action-contracts/`
- `.github/workflows/ci.yml`

## 2026-09-08 - Bound organization data and agent sessions to opaque mediation scopes

**Result**

- A coordinated public package now defines independently selectable contracts
  for calendar reads/writes, conversation-scoped group notes, read-only work
  recommendations, external-memory recall/candidate proposals, and governed
  DSH agent sessions.
- Every request and result is correlated to deployment-, audience-,
  conversation-, target-, request-, workspace-, session-, and prior-receipt
  references where applicable. Mutations use required idempotency and
  approval-classified descriptor actions; conversation-only profiles select
  none of these capabilities.
- Agent-session receipts make cancellation, timeout, terminal state, revision,
  and restart recovery explicit while leaving the agent loop and persistence in
  DSH. External-memory writes are candidate proposals only and do not become a
  second memory store.

**Why / context**

- A channel assistant needs useful organization and coding outcomes without
  receiving provider credentials, raw data targets, a model-chosen working
  directory, ambient shell access, or authority over another conversation.
- Existing community plugins either own provider credentials/storage/UI and a
  broader tool surface, target a different DSH line, or are empty placeholders.
  The official DSH agent/session/workspace primitives remain the implementation
  reference for session lifecycle rather than being duplicated here.

**Evidence**

- Owner tests first failed because the new package did not exist, then passed
  with allowed and denied coverage for each capability family, cross-scope and
  target substitution, stale/replayed receipts, terminal-state actions,
  cancellation, timeout, restart recovery, and capability non-discovery.
- Five plugin-evaluation records preserve the npm/GitHub/official-tree survey,
  exact published-byte inspection, disabled composition probes, and final
  build/reference decisions.
- The focused exact-owner suite, full repository suite, typecheck,
  compatibility manifest check, reproducible package verification, and dry-run
  package inspection pass.

**Links**

- Issue #25
- `packages/governed-action-contracts/`
- `docs/plugin-evaluations/`

## 2026-09-08 - Telegram audiences become exact, replay-fenced dispatch contracts

**Result**

- The public Telegram package now accepts only deployment-authenticated opaque
  audience results, independently checks participant authorization and exact
  conversation admission, exact-compares authenticated mention/reply
  addressing, and rejects missing, malformed, mismatched, denied, or
  already-consumed bindings before dispatch.
- One transport can select private DM, mention-gated group, or free-response
  group behavior per admitted conversation. Reply-to-bot continuation counts
  as addressing, and every group binding carries explicit public maxima for
  ambient retention, message count, and character count.
- Session keys are isolated by opaque deployment scope and conversation;
  deployment-selected model routes remain inside the public
  `conversation-bounded` ceiling and receive their own isolation keys. The
  public profile remains disabled and contains no credential, channel
  identifier, workspace, tool, skill, shell, media, OCR, or screen authority.
- The owner API now publishes exact denial, admitted-binding, consumption
  request, and consumption-receipt types. One owner-selected timeout bounded by
  the public ceiling and an optional caller cancellation signal cover both
  authorization and atomic consumption.

**Why / context**

- The adopted Telegram transport separates participants with an allowlist but
  exposes only one global group-mention switch. It cannot admit exact chats or
  express mixed mention and free-response behavior for one bot.
- Binding authentication and atomic replay consumption remain runtime/private
  owner responsibilities. The public router validates the exact opaque result
  and derives dispatch keys without retaining a second policy or replay store.

**Evidence**

- A narrow temporary mutation removed only the authenticated addressing
  comparison. The focused owner assertion then failed because an altered
  mention-gated envelope dispatched instead of returning `binding-mismatch`;
  restoring the check made the same assertion pass. The owner suite also
  covers exact denial/receipt shapes, complete consume tuples, bounded timeout
  and cancellation, replay, ceilings, and pairwise participant/session and
  conversation/model-route isolation.
- A fresh exact DSH 0.1.1-rc.2 plugin probe kept
  `@ashafizullah/dsh-telegram@0.5.1` fail-closed and disabled. The multi-agent
  fallback also probed, while `dsh-channel-telegram@0.5.0` could not resolve
  its DSH 0.1.2 peer line against the repository lock.
- Focused Telegram tests and strict TypeScript checks pass. The repository's
  full test, compatibility, reproducibility, and packaging gates provide the
  delivery evidence.

**Links**

- Issue #27
- `packages/telegram-channel/`
- `docs/plugin-evaluations/telegram-audience-routing.md`

**Follow-ups**

- Issue #28 owns the separate media, vision, and Telegram location authority
  review; those surfaces remain disabled here.

## 2026-09-07 - Coordinate the Telegram catalog as release 0.4.0

**Result**

- The workspace, all public packages, native Telegram profile, profile catalog,
  and compatibility lock now carry coordinated version `0.4.0`.
- Every profile digest and the catalog digest were recomputed after the version
  transition. Exact workspace dependencies advance together while the public
  pre-1.0 compatibility ranges remain unchanged.

**Why / context**

- The Telegram composition adds a public profile and package contract. Under
  the repository's pre-1.0 Semantic Versioning policy, that additive public
  surface requires a minor release rather than mutating immutable `v0.3.0`
  artifacts.

**Evidence**

- The focused repository, profile, GitHub, and conversation contracts pass.
- The full test and typecheck gates, manifest and exact-checkout compatibility,
  reproducible packaging, package inspection, and exact DSH native Telegram
  composition all pass at version `0.4.0`.

**Links**

- PR #23
- `compatibility/dsh-applications-lock.json`
- `profiles/catalog.json`

## 2026-09-07 - Native Telegram install consumes a committed npm graph

**Result**

- The native `telegram-conversational` profile now ships an npm v3 lockfile for
  its complete dependency graph and requires `npm ci --ignore-scripts` from a
  clean profile directory.
- Owner coverage verifies the selected Telegram package's reviewed integrity,
  the exact DSH 0.1.1-rc.2 package family, a successful clean install, and the
  absence of lifecycle-script execution. Exact-compatibility CI also loads the
  installed profile through the pinned DSH launcher and verifies its sole
  disabled Telegram mount and fail-closed optional surfaces. The coordinated
  package allowlist now requires the nested lockfile in the release artifact.

**Why / context**

- Exact top-level dependency selectors did not freeze ranged transitive
  dependencies. The separate external-plugin identity record documented the
  reviewed Telegram tarball, but npm could not consume that record when
  resolving an installation.
- `npm ci` makes the checked-in graph executable policy: it refuses manifest
  drift instead of silently refreshing the graph. `--ignore-scripts` remains
  explicit even though the profile also carries the same local npm default.

**Evidence**

- The new owner test first failed before installation because the nested lock
  was absent. Against the repaired profile it copies only the native fragment
  to a fresh temporary directory, runs `npm ci --ignore-scripts`, and verifies
  the installed package identities.
- The test configures an intentionally unusable lifecycle shell; the clean
  install still succeeds and no shell artifact appears, confirming npm did not
  execute dependency lifecycle scripts.
- A repository-contract test first failed because exact-compatibility CI did
  not invoke the native Telegram composition owner test. The repaired workflow
  runs that test against the exact DSH checkout after the locked clean install.

**Links**

- PR #23
- `profiles/telegram-conversational/dsh-profile/package-lock.json`
- `test/telegram-conversation.test.ts`

## 2026-09-07 - Telegram conversation becomes a fail-closed public composition

**Result**

- `packages/telegram-channel` now describes the exact reviewed
  `@ashafizullah/dsh-telegram@0.5.1` artifact as a runtime-kit plugin contract;
  it exposes no agent tools or skills and requests only bounded instance state,
  Telegram API networking, and a bot-token credential-handle class.
- `profiles/telegram-conversational` composes that channel with the existing
  conversation agent while granting only `conversation.memory` and
  `conversation.reply`. Its native DSH mount is disabled and its public
  defaults turn off attachment ingestion, OCR, and screen capture until private
  admission supplies the credential handle, access bindings, isolated
  workspace, conversation-only agent preset, and explicit enablement.
- The input contract reuses the channel-neutral keyed opaque refs. No
  credential reference, raw channel or participant identifier, host binding,
  private source path, or rollout state enters the public composition.

**Why / context**

- The reusable public layer needed a concrete Telegram ingress/reply
  composition without absorbing personal deployment topology or duplicating
  DSH's agent loop and runtime-kit's policy, admission, lifecycle, and receipt
  ownership.
- The ecosystem evaluation was rerun against the exact locked DSH 0.1.1-rc.2
  launcher. The selected package remains the smallest compatible fit; the
  recorded fallback also probes successfully, while one alternative still has
  an unresolvable peer graph on the locked release-candidate line.
- Native acceptance exposed an important installation constraint: DSH's plugin
  management command registers the upstream Telegram bundle as active
  composition. The portable fragment therefore installs exact dependencies
  directly with lifecycle scripts disabled and keeps Telegram out of
  `dsh.profile.bundles`; its explicit disabled insert is the sole mount.

**Evidence**

- The owner test first failed because the Telegram profile, external-artifact
  lock, descriptor, and schema did not exist, then passed all four bounded
  composition and privacy cases against the exact runtime-kit checkout.
- A fresh throwaway DSH home installed the exact native dependencies with
  lifecycle scripts blocked; the exact locked launcher dumped one Telegram
  mount, with `disabled: true` and `enabled: false`.
- The full repository test suite, TypeScript checks, exact-checkout and
  manifest-only compatibility checks, reproducible-package verification, and
  package dry-run pass.

**Links**

- `profiles/telegram-conversational/`
- `packages/telegram-channel/`
- `docs/plugin-evaluations/telegram-channel.md`

## 2026-09-05 - Repository scripts and tests move to TypeScript

**Result**

- All 22 repository-owned Node.js entrypoints under `scripts/` and `test/`
  now use directly executable `.ts` files; the workspace no longer carries
  `.js`, `.mjs`, or `.cjs` source files.
- `npm run typecheck` now checks both the strict production package program and
  a separate tools/test program. A repository contract prevents JavaScript
  entrypoints from returning and verifies that scripts and tests stay in the
  TypeScript program.

**Why / context**

- Node.js 24 already executes the workspace's erasable package TypeScript, so
  retaining a second source format for release checks and tests added no runtime
  compatibility value. Converging the repository on `.ts` makes import paths,
  editor tooling, and compiler coverage consistent without adding a build step.
- Runtime contract tests deliberately construct malformed values and dynamic
  owner stand-ins. `tsconfig.tools.json` keeps those fixtures typechecked with
  explicit boundary casts while `tsconfig.json` continues to enforce the full
  strict contract for shipped package sources.
- Three `.mjs` strings remain intentionally: they resolve the exact external
  runtime-kit checkout's compiled test helper and are not repository-owned
  entrypoints.

**Evidence**

- The repository contract first failed while listing all 22 legacy entrypoints,
  then passed after the conversion.
- `npm test`, `npm run typecheck`, manifest-only compatibility validation,
  reproducible-package verification, and package dry-run all pass on the
  repository-pinned Node.js runtime.

**Links**

- `README.md`, `CONTRIBUTING.md`, and `tsconfig.tools.json` describe the current
  directly executable TypeScript boundary.

## 2026-09-05 - Workspace packages become erasable TypeScript with no build step

**Result**

- The six workspace packages (`plugin-sdk`, `manager`, `dsh-rc2-adapter`,
  `conversation-agent`, `github-read`, `github-review-publish`) are now
  `src/index.ts` sources that Node.js 24 executes directly through built-in
  type stripping (issue #20). Each package's `exports` points at the `.ts`
  file; the hand-written `index.d.ts` files are gone and their types live in
  the source next to the code they describe.
- Nothing about install, packaging, or release changed shape: there is still no
  `build`, `prepare`, or `prepack` script, `npm pack --ignore-scripts` ships
  the reviewed source (66 files, down from 72 without the declaration files),
  and the package-reproducibility check keeps its meaning. `npm run typecheck`
  (`tsc --noEmit`, TypeScript 6.0.3 pinned exactly) joins `npm test` in CI and
  in the release verify job.

**Why / context**

- The declaration files were a second description of each contract that could
  drift from the implementation with no test noticing; the exact-DSH consumer
  check compiled against the `.d.ts` surface, not the code. Folding the types
  into the source puts each public signature next to the implementation it
  describes, so both are reviewed together, and makes every internal validator
  visible to the compiler. The overload-above-implementation pattern still
  leaves overload/implementation agreement to the test suite rather than the
  compiler; what changed is that the two can no longer be edited apart.
- Upstream DSH ships `tsc` plus `tsdown` output under `lib/`; that layout was
  deliberately not adopted. A build output would put generated code between the
  reviewed source and the shipped tarball, which the reproducibility and
  reviewed-source release gates are built to exclude. Erasable syntax
  (`erasableSyntaxOnly`, `verbatimModuleSyntax`) keeps Node's stripping the
  only transform, so Node.js 24 is now a runtime requirement, not only a
  development one.
- The public signatures are carried verbatim from the old declaration files as
  overloads above each implementation, so the exported surface is reviewable
  line by line against the deleted `.d.ts`. The optional DSH peer packages the
  adapter binds its types to are not installed in this repository, so a minimal
  ambient fallback shipped with the adapter (`types/dsh-peer-fallbacks.d.ts`)
  lets `npm run typecheck` run standalone. An ambient declaration shadows the
  installed package in any program that includes it, so
  `npm run test:exact-dsh` compiles a separate program that excludes the
  fallback and maps the peer names to the pinned DSH checkout; that run remains
  the authority for the DSH-facing surface.
- Two consumer-facing consequences of shipping `.ts` came out of review and are
  now documented in the README: Node refuses to strip types under
  `node_modules`, so the packages load only from an unpacked artifact root; and
  a TypeScript consumer compiles the sources under its own options, so the
  sources are held to `noUncheckedIndexedAccess` and
  `exactOptionalPropertyTypes` as well, and a checked consumer fixture compiles
  them without the DSH peers in the repository contract test.
- This is the pilot for the same conversion in dsh-runtime-kit
  (sympoies/dsh-runtime-kit#202), which carries a hosted gate cycle this
  repository does not.

**Evidence**

- Zero specification edits: every existing test passes with only import-path
  changes, 94 pass / 0 fail / 1 environment-skip against the locked runtime-kit
  checkout on Node.js 24.16.0, and the same result on Node.js 26.
- `npm run test:exact-dsh` against the pinned DSH 0.1.1-rc.2 checkout: DSH's own
  `tsc` 6.0.3 resolves the `.ts` export targets and type-checks the adapter and
  its consumers strictly; the live agent tool round-trip passes.
- `tsc --declaration --emitDeclarationOnly` over the new sources, normalized for
  whitespace and the `declare` keyword, is identical to the deleted `.d.ts`
  files except that `EFFECTFUL_HOST_ACTION_CLASSES` is now declared (it was
  exported at runtime but never typed).
- Importing every package under Node.js 24.16.0 writes nothing to stderr; no
  `ExperimentalWarning` or stripping diagnostic appears in any test run.
- `git diff -M40%` pairs all six sources as renames (42% to 74% similarity; the
  smallest files gain the most type text relative to their size).

## 2026-09-01 - Conversation agent realizes the conversational profile

**Result**

- `packages/conversation-agent` (PR #19) implements the turn and reply
  contracts the `conversational` profile had been declaring against a plugin
  that no package provided, so the profile moves from a validated shape to a
  realizable contract.
- A turn may now carry channel context — `chatRef`, `senderRef`, `isGroup` —
  where the refs are opaque deployment-scoped digests, letting one agent serve
  group and direct chats on any channel without a real identifier entering the
  public repository.

**Why / context**

- The missing piece for a Telegram or LINE agent was never channel-specific:
  ingress and delivery are adapter-owned, so the reusable gap was the
  conversation contract itself.
- The profile had declared `conversation-agent >=1.0.0 <2.0.0`, which the
  coordinated 0.3.0 workspace version can never satisfy; the declaration had
  never been exercised because no package existed to check it against.

**Evidence**

- Independent review found five issues, all repaired before merge. Two were
  contract defects that testing alone had missed: a getter could pass the ref
  check and then substitute a raw chat id into the returned document,
  defeating the privacy guarantee; and text bounds counted UTF-8 bytes while
  the digest-pinned schemas declare `maxLength` in code points, so a 6000
  character CJK message satisfied the published contract yet was rejected at
  runtime.
- The keyed-digest requirement for refs is now normative in the schema field
  descriptions rather than README prose, so it travels with the digest-pinned
  contract; a bare hash of a numeric chat id is exhaustively invertible.
- 93 pass / 0 fail against the locked runtime-kit checkout, and
  `check-profile-compatibility` returns ok against a real git checkout at the
  locked revision, validating the recomputed profile, catalog, and lock digest
  chain.

**Follow-ups**

- The private deployment owns minting refs by HMAC over a deployment secret;
  an unsalted adapter would satisfy the shape while breaking the guarantee.
- `freezeClone` over caller-owned memory is shared with the other public
  packages, which carry no privacy claim today but would inherit the same
  substitution weakness if one were added.

## 2026-09-01 - Evaluate-before-build skill established; Telegram channel adopted

**Result**

- The `evaluate-dsh-plugin` skill (PR #17) now gates new capabilities on an
  ecosystem survey, fixed evaluation criteria, a throwaway-DSH_HOME probe
  (`scripts/dsh-plugin-probe.sh`), and a recorded adopt/reference/build
  decision under `docs/plugin-evaluations/`.
- Its first application decided the Telegram channel: adopt
  `@ashafizullah/dsh-telegram@0.5.1` (`docs/plugin-evaluations/telegram-channel.md`).

**Why / context**

- The DSH plugin ecosystem is large enough (13k+ GitHub `dsh-plugin` repos, a
  curated awesome list, thousands of npm packages) that building from scratch
  without a survey risks reinventing maintained work.
- The probe earns its keep: its fail-closed observation caught
  `dsh-telegram-multiagent` self-mounting **enabled** via its shipped patch
  layer after `dsh plugin add` silently bundle-registered it — exactly the
  class of supply-chain surprise the skill exists to surface before a real
  profile is touched.

**Evidence**

- Live probes against locked DSH 0.1.1-rc.2: two finalists PASS
  (fail-closed + disabled activation proven), two candidates FAIL on
  unresolvable prerelease peer ranges; owner test and full suite green on
  PR #17's repaired head.

**Links**

- PR #17 (skill + probe), `docs/plugin-evaluations/telegram-channel.md`
  (survey, probe transcripts summary, decision, follow-ups).

**Follow-ups**

- Public conversational Telegram profile composing the adopted plugin;
  private credential/ingress binding in the deployment repository; a future
  LINE record (no ecosystem candidate exists).
